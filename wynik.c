#include <stdio.h>

// Funkcja do obliczania wyniku
void obliczWynik(int odsłoniętePola, int poziomTrudnosci) {
    int wynik = odsłoniętePola * poziomTrudnosci;
    printf("Aktualny wynik: %d\n", wynik);
}

